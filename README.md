# avc_sec_example
Exec resource examples and powershell facts

## reg_test
An example of checking a Windows registry value and set it if not at the desired value

## spi_test
Same as reg_test but spi timer now with variable setting
